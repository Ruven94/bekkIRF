% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bekk_bootstrap.R
\name{bekk_bootstrap}
\alias{bekk_bootstrap}
\title{Bootstrap BEKK model parameters}
\usage{
bekk_bootstrap(
  bekk_model,
  bekk_spec_model = NULL,
  data = NULL,
  bootsamp = 999,
  cores = max(1L, parallel::detectCores() - 1L),
  root_type = c("spectral", "cholesky", "spec", "chol"),
  seed = 123L,
  max_iter = 50,
  crit = 1e-09,
  progress = TRUE,
  chunk_size = NULL
)
}
\arguments{
\item{bekk_model}{A fitted BEKK model object with entries \code{H_t}, \code{data},
\code{C0}, \code{A}, \code{G}, and optionally \code{B}, \code{asymmetric}, \code{signs}, and \code{spec}.}

\item{bekk_spec_model}{BEKK specification used for re-estimation. If \code{NULL},
\code{bekk_model$spec} is used.}

\item{data}{Numeric return matrix. If \code{NULL}, \code{bekk_model$data} is used.}

\item{bootsamp}{Number of bootstrap replications.}

\item{cores}{Number of parallel workers.}

\item{root_type}{Matrix root used to standardize and simulate innovations.
Use \code{"spectral"}/\code{"spec"} or \code{"cholesky"}/\code{"chol"}.}

\item{seed}{Integer seed. Replication \code{b} uses \code{seed + b}.}

\item{max_iter}{Maximum number of BEKK optimizer iterations passed to
\code{\link[BEKKs:bekk_fit]{BEKKs::bekk_fit()}}.}

\item{crit}{Convergence criterion passed to \code{\link[BEKKs:bekk_fit]{BEKKs::bekk_fit()}}.}

\item{progress}{Logical. If \code{TRUE}, print a text progress bar.}

\item{chunk_size}{Number of bootstrap replications submitted per parallel
batch. The default uses \code{cores}.}
}
\value{
An object of class \code{"bekkBootstrap"} containing bootstrap parameter
arrays \code{C0}, \code{A}, \code{G}, optionally \code{B}, and diagnostic vectors \code{converged}
and \code{error}.
}
\description{
Simulates bootstrap return paths from a fitted BEKK model by resampling the
fitted standardized innovations, refits the supplied BEKK specification to
each simulated path, and returns bootstrap parameter arrays.
}
