% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_irf.R
\name{compute_irf}
\alias{compute_irf}
\title{Compute simulation-based BEKK impulse response functions}
\usage{
compute_irf(
  bekk_model,
  root_type = c("spectral", "cholesky", "spec", "chol"),
  shock_type = c("structural", "empirical"),
  shock = NULL,
  time = NULL,
  simsamp = 10000,
  n.ahead = 100,
  seed = 123,
  calc_virf = TRUE,
  calc_cirf = TRUE,
  calc_kirf = FALSE,
  calc_sirf = FALSE,
  calc_wirf = FALSE,
  bekk_bootstrap = NULL,
  ci_level = 0.95,
  bootstrap_cores = max(1L, parallel::detectCores() - 1L, na.rm = TRUE),
  bootstrap_progress = TRUE,
  bootstrap_chunk_size = NULL
)
}
\arguments{
\item{bekk_model}{A fitted BEKK model object with entries \code{H_t}, \code{data},
\code{C0}, \code{A}, \code{G}, and optionally \code{B}, \code{asymmetric}, and \code{signs}.}

\item{root_type}{Matrix root used to map standardized innovations into
shocks. Use \code{"spectral"}/\code{"spec"} or \code{"cholesky"}/\code{"chol"}.}

\item{shock_type}{Either \code{"structural"} for a user supplied standardized
shock or \code{"empirical"} to use \code{xi[time, ]}.}

\item{shock}{Numeric standardized shock vector. Required for
\code{shock_type = "structural"}.}

\item{time}{Initial time index used for \code{H_0} and empirical shocks.}

\item{simsamp}{Number of Monte Carlo paths.}

\item{n.ahead}{Number of horizons.}

\item{seed}{Integer random seed.}

\item{calc_virf, calc_cirf, calc_kirf, calc_sirf, calc_wirf}{Logical flags for
the requested IRF types.}

\item{bekk_bootstrap}{Optional \code{"bekkBootstrap"} object. If supplied,
\code{compute_irf()} recomputes the selected IRFs for each converged bootstrap
parameter draw and returns pointwise bootstrap confidence intervals.}

\item{ci_level}{Confidence level for central bootstrap intervals, e.g.
\code{0.95}, or a length-two numeric vector of quantile probabilities.}

\item{bootstrap_cores}{Number of parallel workers used for bootstrap IRF
paths. Ignored if \code{bekk_bootstrap = NULL}.}

\item{bootstrap_progress}{Logical. If \code{TRUE}, print a text progress bar for
bootstrap IRF paths.}

\item{bootstrap_chunk_size}{Number of bootstrap IRF paths submitted per
parallel batch. The default uses \code{bootstrap_cores}.}
}
\value{
A list with mean IRF matrices for the selected IRF types.
}
\description{
Simulates paired non-shock and shock BEKK paths from one initial covariance
matrix and averages the resulting differences over Monte Carlo paths.
}
